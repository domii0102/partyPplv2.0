
export const userRoleOptions = {
    ADMIN: "admin",
    USER: "user"
};

export const eventStatusOptions = {
    UPCOMING: "upcoming",
    ONGOING: "ongoing",
    FINISHED: "finished"
}

export const inviteStatusOptions = {
    PENDING: "pending",
    ACCEPTED: "accepted",
    REJECTED: "rejected",
    CANCELED: "canceled"
}